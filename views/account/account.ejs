<!-- the content heading -->
<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
        res.redirect('/')
    } %>
    
    <%# messages() %> 
    
    <!-- The login account Display 
    All the logic and building were done previously,
    the view only displays the result.-->
    <%- login %>

    <!-- flash message-->
    <%- messages() %>

    